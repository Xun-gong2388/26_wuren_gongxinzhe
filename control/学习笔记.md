这次的教程简直夯爆了，不需要额外学习其他东西就能完成任务一，任务二在学习了Eigen矩阵的简单知识后就能完成

本次主要对ai的使用是在某些公式的推导（比如Q2里面u = u + u_rate * h(X(0), X(1))，而不是减去h(X(0), X(1))），用到了对偶函数的知识）

还有对于无法收敛，导致出现报错的问题，也是通过ai来寻找更好的解决方法（比如Q1里面一开始用的是x和y作为判断条件，但是出现收敛失败的情况，于是借用了ai给的建议，
把判断条件改为了损失函数大小，并且用的是大于0.001，而不是等于0）







每次运行要在终端输入以下命令：g++ main.cpp -o main.exe -I.
                          然后 ./main.exe
# Eigen 学习笔记

Eigen学习网址:https://www.cnblogs.com/clnchanpin/p/19545637

# Eigen 矩阵定义
 Eigen 中矩阵的定义格式为 Matrix<Scalar, Rows, Cols>，其中：

    Scalar：数据类型（如 float、double、int）。

    Rows/Cols：行数 / 列数（Dynamic 表示动态大小，编译时不确定）

 ### 常用别名（简化定义）：

     MatrixXd：动态大小的 double 矩阵（Matrix<double, Dynamic, Dynamic>）。

     Vector3f：3 维 float 向量（Matrix<float, 3, 1>）。

     Matrix2d：2x2 的 double 矩阵（Matrix<double, 2, 2>）。

# 具体构建：
   （一个名为m的2x2 double矩阵）

    Eigen::MatrixXd m(2, 2);

    对于固定大小矩阵（包括 Vector3f（这是三维向量、Matrix2d），可给他直接赋值，如下

    Eigen::Matrix2d m2;  // 2x2 double 矩阵

    m2 << 1, 2,          // 初始化：按行填充

      3, 4;

# 访问：
  Eigen::MatrixXd m(2, 2);

 m(0, 0) = 3; // 通过 (i, j) 访问元素，下标从 0 开始

 m(1, 0) = 2.5;

 m(0, 1) = -1;

 m(1, 1) = m(1, 0) + m(0, 1);

 std::cout << m << std::endl;

 // 输出：

 //  3  -1

 // 2.5 1.5

# 获取矩阵信息： 
   Eigen::Vector3d v(1, 2, 3);


   std::cout << v[0] << ", " << v(1) << std::endl; // 向量可以用 [] 或 () 访问

   // 获取矩阵信息

   int rows = m.rows();

   int cols = m.cols();

   int size = v.size(); // 对于向量